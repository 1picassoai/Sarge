# The series — Wed 16 Sep 2026, one repo, climbing

*The Captain's ruling, ~07:05: "keep giving it more complex tasks on the same repo, at
least another five, and we keep recording." Same folder `repos\myshop`, HARNESS arm, one
Run each, nothing reset between tasks. Every task leans on a rule in the book so the book
is what is under test. Verdicts are the agent's; the by-hand column is mine, checked
after each run. Run facts are from the logs; anything outward is @Gareth's.*

| # | task | run | verdict | by hand | rule under test | notes |
|---|------|-----|---------|---------|-----------------|-------|
| 1 | create WorkshopTools API | 065055 | WORKS | ✔ | scoped-context-in-handler · minimal-api | 58s · student asked once · tutor taught `select-specific-fields` |
| 2 | add GET/PUT /tools/{id} | 065618 | WORKS | ✔ POST/GET/PUT/GET | scoped-context-in-handler on new code | 14s · one edit · 4/4 handlers inject |
| 3 | validation on POST/PUT | 070713 | WORKS | ✔ 3 bad POSTs 400 · good 201 · bad PUT 400 · good 204 | money-is-decimal · minimal-api | 22s · one edit · tutor taught `entity-must-have-isarchived-flag` (pre-empting task 5) |
| 4 | search + paging on GET /tools | 071547 | WORKS | ✔ search case-insensitive · page 2 of 2 · size 500 → 400 | explicit-columns · async-suffix | 34s · 3 edits · forced ask fired once (CS1503/CS1737, nullable int arithmetic) then green · projects Id/Name/Price · chose 400 over capping at 50 |
| 5 | DELETE /tools/{id} as archive | 071951 | **DOES NOT BUILD** | ✘ handler + filters right, `IsArchived` never added to `Tool` | archive-not-delete · define-entities-in-program-cs | 140s · 26 calls · 4× `old_text not found` on the Tool class, then wrote a duplicate `Tool` in a new `Tool.cs` (CS0101) · forced ask ×2, no own ask · 6 builds with no edit · **the check passed a new entity file** — rule weak · design change: 2nd rejection now says "write_file the whole file" |
| 5b | same, rerun after stray file removed | 072434 | **DOES NOT BUILD** | ✘ same shape | archive-not-delete · define-entities-in-program-cs | 120s · 27 calls · wrote `WorkshopTools.cs` with a duplicate Tool+ToolContext (CS0101/CS0111) · edit of its OWN file rejected · forced ask ×2 not followed · **15 builds with no edit** — the idle-build guard did not stop it · never reached the db trap |
| 5c | same, rerun after 2nd stray file removed | 072913 | RUNS BUT FAILS | ◐ `IsArchived` added · DELETE handler added · build green · but `GET /tools` and `GET /tools/{id}` are mapped **twice** — scar from run 5b's first edit → AmbiguousMatchException | archive-not-delete | 90s+ · 2 rejected edits (model unindents `{`, and CRLF) · forced ask ×2 · **own ask once, on the right question** · remaining misses were indentation (brace at column 0) — fallback added |
| 5d | maintenance: remove duplicate mappings | 073353 | RUNS BUT FAILS | ✔ duplicates gone in **one edit**, build green · then **the db trap bit**: `no such column: IsArchived` on the 07:16 `tools.db` | edit tool on a scarred file | 32s · the model diagnosed the stale schema **correctly** in prose, said "I cannot create migrations", and wrote nothing — never asked · first sighting so the net could not fire · tutor taught `never-expose-internal-fields` |
| 5 ✔ | **task 5 code verified by hand** on a fresh db | — | — | ✔ POST ×2 · GET both · DELETE /tools/1 → 204 · GET list shows one · GET /tools/1 → 404 · GET /tools/2 → 200 | archive-not-delete | the model's code for task 5 is correct; what beat it was a stale dev database it had no tool to replace and did not ask about |
| 5e | recreate the dev db at startup **in Development** | 073837 | WORKS | ✘ **wrong** — `EnsureDeleted()` + `EnsureCreated()` run unconditionally, no `IsDevelopment()` guard: the app drops its database on every start, in every environment | core law "never delete data" · tutor's green review | 30s · one edit · endpoints answered so the verdict is green · **the tutor's end-of-task review did not catch a data-destroying line** ("ALREADY HELD never-expose-internal-fields") — the biggest gap of the day: WORKS is not RIGHT, and neither the check nor the review saw it |
| 5f | corrective: guard the drop with IsDevelopment | 074125 | WORKS | ✔ `if (builder.Environment.IsDevelopment()) { EnsureDeleted; EnsureCreated } else { EnsureCreated }` — exactly right | core law "never delete data" | 70s · one rejected edit (whole-file old_text, mismatch) then one good edit · correct when told; the point stands that nobody *told* it in 5e |
| 6 | Category entity + relation | 074959 | RUNS BUT FAILS | ◐ Category entity, `CategoryId`, second DbSet, GET /categories 200, GET /categories/{id}/tools 200 — **POST /categories missing** (added, hit AmbiguousMatch, reverted it instead of fixing) | define-entities-in-program-cs · scoped-context | 181s · 29 calls · 3 idle run_apps · **context blown at 16,397 tokens** · forced ask ×2 · run_app probes GET only, so it could never see POST answer — tool limit |
| 6b | corrective: add POST /categories | 075604 | WORKS | ✔ POST category 201 · GET lists it · tool with categoryId 201 · tool without → null · GET /categories/1/tools returns the one | scoped-context on new code | 21s · **one edit** · each route mapped once · task 6 now complete end to end |
| 7 | GET /tools/stats — **17 Sep, through the MAF bridge** (`Sarge.AgentFramework`, rules via the framework's context provider, 12 held) | 181526 | **WORKS** | ✔ see by-hand line below | money-is-decimal · explicit-columns · archive-not-delete | **29s · 5 calls · one edit · checks passed** · tutor's end review taught `no-auto-drop-database-on-startup` (flagged the `EnsureDeleted` in Development from task 5e, and the in-memory `Sum` — aggregation should be `CountAsync`/`SumAsync` in SQL) · first attempt 181138 was blocked by a **false flag**: the check hit the startup `CreateScope()` line the rule's own `allow` clause names; fixed in `verdict.rs` (ALLOWED is now an answer), replay unchanged 6/7, 0 false |

## The tasks, as pasted

**3.** Add validation to the WorkshopTools API: POST /tools and PUT /tools/{id} must return
400 with a short message when Name is empty or whitespace, or when Price is zero or
negative. Build it, run it, and show a bad request returning 400 and a good one still
working.

**4.** Add search and paging to GET /tools: optional query parameters `search` (matches
Name, case-insensitive), `page` (default 1) and `size` (default 10, max 50). Return only Id,
Name and Price. Build it, run it, and show GET /tools?search=ham&page=1&size=5 answering.

**5.** Add DELETE /tools/{id}. This codebase never hard-deletes: add an IsArchived flag to
Tool, DELETE sets it and returns 204, and GET /tools and GET /tools/{id} no longer return
archived tools. Build it, run it, and show a tool disappearing from GET after DELETE.

**6.** Add a Category entity (Id, Name) and give each Tool an optional CategoryId. Add GET
/categories, POST /categories, and GET /categories/{id}/tools returning the tools in that
category. Keep everything in Program.cs. Build it, run it, and show the three endpoints
answering.

**7.** Add GET /tools/stats returning the number of tools that are not archived and the
total of their prices as a decimal. Build it, run it, and show it answering.
